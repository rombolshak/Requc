<UserControl x:Class="Requc.Views.ProtocolActView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
           <!-- <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>-->
        </Grid.ColumnDefinitions>
        <!--<ContentPresenter Content="{Binding Path=AliceProtocolDevice}" Grid.Column="0" HorizontalAlignment="Left" />
        <ContentPresenter Content="{Binding Path=EnvironmentDevice}" Grid.Column="1" HorizontalAlignment="Left" />
        <ContentPresenter Content="{Binding Path=BobProtocolDevice}" Grid.Column="2" HorizontalAlignment="Right"/>-->
        <ContentPresenter Content="{Binding Path=AllInOneDevice}" Grid.Column="1" HorizontalAlignment="Center" />
    </Grid>
</UserControl>

<UserControl x:Class="Requc.Views.SimpleProtocolView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="500">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <ContentPresenter Content="{Binding Path=ProtocolAct.AliceProtocolDevice}" Grid.Column="0"/>
        <ContentPresenter Content="{Binding Path=ProtocolAct.BobProtocolDevice}" Grid.Column="1"/>
        <StackPanel Grid.Column="2">
            <Button Height="100" Command="{Binding Path=RunCommand}" CommandParameter="{Binding Path=ProtocolAct}">Run</Button>
        </StackPanel>
    </Grid>
</UserControl>
